# Core dependencies (Python 3.11)
cobra>=0.29.0          # metabolic network modeling & flux analysis
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0          # used by ml_svm.py
openpyxl>=3.1          # xlsx read/write
joblib>=1.3
matplotlib>=3.7        # visualization
shap>=0.44             # interpretability for ml_best_model_analysis.py
scipy>=1.10

# Solvers (required for the MOMA QP; install at least one)
gurobi>=10.0           # Recommended: commercial / academic license
# optlang>=1.8         # COBRApy default (GLPK)
